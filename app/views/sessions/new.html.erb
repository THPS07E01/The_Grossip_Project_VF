<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<div class="d-flex justify-content-center mt-3">

  <h1 class="text-primary">CONNEXION</h1><br/>

	<br/>


</div>
<div class="d-flex justify-content-center">
	<h4 class="text-secondary text-center">Viens voir le docteur.</h4>
	<br/>
</div>

<br/>

<section class="d-flex justify-content-center">

<form action='/sessions/' method='POST'>
      <input type='hidden' name='authenticity_token' value='<%= form_authenticity_token %>'>

      <div class="d-flex justify-content-center">
      <input type='text' name='user_email' placeholder='Ton email'/><br/>
      </div>
      <div class="d-flex justify-content-center">
      <input type='text' name='user_password' placeholder='Mot de passe'/><br/>
      </div>
      <br/>
       <div class="d-flex justify-content-center">
      <input class="btn btn-primary" type='submit' value='Envoyer'/>
      </div>
</form>

</section>

<section class="d-flex justify-content-center mt-3">
<a href="<%= new_user_path %>">Je n'ai pas de compte</a>
</section>
