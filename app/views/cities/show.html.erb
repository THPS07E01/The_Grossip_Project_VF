<div class="d-flex justify-content-center mt-3">

  <h1 class="text-primary"><%=City.find(params[:id]).name %></h1><br/>
	<br/>
	
</div>
<div class="d-flex justify-content-center">
	<h4 class="text-secondary text-center">Cette jolie petite bourgade est la ville numéro <%= City.find(params[:id]).id %> de notre belle base de données.</h4>
	<br/>
	
</div>

<br/>